---
title: "Obtener Pasos"
description: "Lista los pasos disponibles de un negocio para su uso en campañas"
api: "POST /campanias/obtener/pasos"
---

Retorna todos los pasos activos de un negocio. Los pasos representan etapas de un flujo o embudo (pipeline) y se usan como valor del campo `step` al enviar una campaña masiva por plantilla.

## Autenticación

Requiere el encabezado `x-api-key` con el App Key del negocio. Consulta [Autenticación](/authentication).

## Encabezados

<ParamField header="x-api-key" type="string" required>
  App Key del negocio. Consulta [Autenticación](/authentication).
</ParamField>

## Cuerpo de la solicitud

<ParamField body="empresa_id" type="number" required>
  ID del negocio cuyos pasos deseas obtener.
</ParamField>

## Respuesta

<ResponseField name="pasos" type="array">
  Lista de pasos disponibles para el negocio.

  <Expandable title="Objeto de paso">
    <ResponseField name="id" type="number">ID interno del paso. Usa este valor en el campo `step` al crear una campaña.</ResponseField>
    <ResponseField name="nombre" type="string">Nombre descriptivo del paso.</ResponseField>
    <ResponseField name="empresa_id" type="number">Negocio al que pertenece el paso.</ResponseField>
    <ResponseField name="deleted_at" type="string | null">Marca de eliminación suave. `null` para pasos activos.</ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>
```bash
curl -X POST https://beta.api-iobot-desarrollo.com/campanias/obtener/pasos \
  -H "Content-Type: application/json" \
  -H "x-api-key: TU_APP_KEY" \
  -d '{
    "empresa_id": 1
  }'
```
</RequestExample>

<ResponseExample>
```json 200
{
  "pasos": [
    {
      "id": 3,
      "nombre": "Prospecto",
      "empresa_id": 1,
      "deleted_at": null
    },
    {
      "id": 7,
      "nombre": "Negociación",
      "empresa_id": 1,
      "deleted_at": null
    },
    {
      "id": 12,
      "nombre": "Cierre",
      "empresa_id": 1,
      "deleted_at": null
    }
  ]
}
```

```json 400 Parámetros inválidos
{
  "success": false,
  "error": "empresa_id es requerido"
}
```

```json 500 Error inesperado
{
  "success": false,
  "error": "Error: ..."
}
```
</ResponseExample>
